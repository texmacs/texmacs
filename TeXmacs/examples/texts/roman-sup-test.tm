<TeXmacs|1.99.6>

<style|generic>

<\body>
  <\equation*>
    aa<rsup|A>+ab<rsup|A>+ac<rsup|A>+ad<rsup|A>+ae<rsup|A>+af<rsup|A>+ag<rsup|A>+ah<rsup|A>+ai<rsup|A>+aj<rsup|A>
  </equation*>

  <\equation*>
    ak<rsup|A>+al<rsup|A>+am<rsup|A>+an<rsup|A>+ao<rsup|A>+ap<rsup|A>+aq<rsup|A>+ar<rsup|A>+as<rsup|A>+at<rsup|A>
  </equation*>

  <\equation*>
    au<rsup|A>+av<rsup|A>+aw<rsup|A>+ax<rsup|A>+ay<rsup|A>+az<rsup|A>
  </equation*>

  <\equation*>
    AA<rsup|A>+AB<rsup|A>+AC<rsup|A>+AD<rsup|A>+AE<rsup|A>+AF<rsup|A>+AG<rsup|A>+AH<rsup|A>
  </equation*>

  <\equation*>
    AI<rsup|A>+AJ<rsup|A>+AK<rsup|A>+AL<rsup|A>+AM<rsup|A>+AN<rsup|A>+AO<rsup|A>+AP<rsup|A>
  </equation*>

  <\equation*>
    AQ<rsup|A>+AR<rsup|A>+AS<rsup|A>+AT<rsup|A>+AU<rsup|A>+AV<rsup|A>+AW<rsup|A>+AX<rsup|A>+AY<rsup|A>+AZ<rsup|A>
  </equation*>

  <\equation*>
    aa<rsup|2>+ab<rsup|2>+ac<rsup|2>+ad<rsup|2>+ae<rsup|2>+af<rsup|2>+ag<rsup|2>+ah<rsup|2>+ai<rsup|2>+aj<rsup|2>
  </equation*>

  <\equation*>
    ak<rsup|2>+al<rsup|2>+am<rsup|2>+an<rsup|2>+ao<rsup|2>+ap<rsup|2>+aq<rsup|2>+ar<rsup|2>+as<rsup|2>+at<rsup|2>
  </equation*>

  <\equation*>
    au<rsup|2>+av<rsup|2>+aw<rsup|2>+ax<rsup|2>+ay<rsup|2>+az<rsup|2>
  </equation*>

  <\equation*>
    AA<rsup|2>+AB<rsup|2>+AC<rsup|2>+AD<rsup|2>+AE<rsup|2>+AF<rsup|2>+AG<rsup|2>+AH<rsup|2>
  </equation*>

  <\equation*>
    AI<rsup|2>+AJ<rsup|2>+AK<rsup|2>+AL<rsup|2>+AM<rsup|2>+AN<rsup|2>+AO<rsup|2>+AP<rsup|2>
  </equation*>

  <\equation*>
    AQ<rsup|2>+AR<rsup|2>+AS<rsup|2>+AT<rsup|2>+AU<rsup|2>+AV<rsup|2>+AW<rsup|2>+AX<rsup|2>+AY<rsup|2>+AZ<rsup|2>
  </equation*>

  <\equation*>
    aa<rsup|c>+ab<rsup|c>+ac<rsup|c>+ad<rsup|c>+ae<rsup|c>+af<rsup|c>+ag<rsup|c>+ah<rsup|c>+ai<rsup|c>+aj<rsup|c>
  </equation*>

  <\equation*>
    ak<rsup|c>+al<rsup|c>+am<rsup|c>+an<rsup|c>+ao<rsup|c>+ap<rsup|c>+aq<rsup|c>+ar<rsup|c>+as<rsup|c>+at<rsup|c>
  </equation*>

  <\equation*>
    au<rsup|c>+av<rsup|c>+aw<rsup|c>+ax<rsup|c>+ay<rsup|c>+az<rsup|c>
  </equation*>

  <\equation*>
    AA<rsup|c>+AB<rsup|c>+AC<rsup|c>+AD<rsup|c>+AE<rsup|c>+AF<rsup|c>+AG<rsup|c>+AH<rsup|c>
  </equation*>

  <\equation*>
    AI<rsup|c>+AJ<rsup|c>+AK<rsup|c>+AL<rsup|c>+AM<rsup|c>+AN<rsup|c>+AO<rsup|c>+AP<rsup|c>
  </equation*>

  <\equation*>
    AQ<rsup|c>+AR<rsup|c>+AS<rsup|c>+AT<rsup|c>+AU<rsup|c>+AV<rsup|c>+AW<rsup|c>+AX<rsup|c>+AY<rsup|c>+AZ<rsup|c>
  </equation*>

  <\equation*>
    aa<rsup|p>+ab<rsup|p>+ac<rsup|p>+ad<rsup|p>+ae<rsup|p>+af<rsup|p>+ag<rsup|p>+ah<rsup|p>+ai<rsup|p>+aj<rsup|p>
  </equation*>

  <\equation*>
    ak<rsup|p>+al<rsup|p>+am<rsup|p>+an<rsup|p>+ao<rsup|p>+ap<rsup|p>+aq<rsup|p>+ar<rsup|p>+as<rsup|p>+at<rsup|p>
  </equation*>

  <\equation*>
    au<rsup|p>+av<rsup|p>+aw<rsup|p>+ax<rsup|p>+ay<rsup|p>+az<rsup|p>
  </equation*>

  <\equation*>
    AA<rsup|p>+AB<rsup|p>+AC<rsup|p>+AD<rsup|p>+AE<rsup|p>+AF<rsup|p>+AG<rsup|p>+AH<rsup|p>
  </equation*>

  <\equation*>
    AI<rsup|p>+AJ<rsup|p>+AK<rsup|p>+AL<rsup|p>+AM<rsup|p>+AN<rsup|p>+AO<rsup|p>+AP<rsup|p>
  </equation*>

  <\equation*>
    AQ<rsup|p>+AR<rsup|p>+AS<rsup|p>+AT<rsup|p>+AU<rsup|p>+AV<rsup|p>+AW<rsup|p>+AX<rsup|p>+AY<rsup|p>+AZ<rsup|p>
  </equation*>

  \;

  <\equation*>
    \<ell\>=log<rsup|2> x=log<rsup|A> x=log<rsup|c> x=log<rsup|B>
    x=log<rsup|b> x
  </equation*>

  <\equation*>
    \<ell\>=loq<rsup|2> x=loq<rsup|A> x=loq<rsup|c> x=loq<rsup|B>
    x=loq<rsup|b> x
  </equation*>

  \;

  <\equation*>
    X<rsup|aa>+X<rsup|bb>+X<rsup|cc>+X<rsup|dd>+X<rsup|ee>+X<rsup|ff>+X<rsup|gg>+X<rsup|hh>+X<rsup|ii>+X<rsup|jj>
  </equation*>

  <\equation*>
    X<rsup|kk>+X<rsup|ll>+X<rsup|mm>+X<rsup|nn>+X<rsup|oo>+X<rsup|pp>+X<rsup|qq>+X<rsup|rr>+X<rsup|ss>+X<rsup|tt>
  </equation*>

  <\equation*>
    X<rsup|uu>+X<rsup|vv>+X<rsup|ww>+X<rsup|xx>+X<rsup|yy>+X<rsup|zz>
  </equation*>

  <\equation*>
    X<rsup|AA>+X<rsup|BB>+X<rsup|CC>+X<rsup|DD>+X<rsup|EE>+X<rsup|FF>+X<rsup|GG>+X<rsup|HH>+X<rsup|II>+X<rsup|JJ>
  </equation*>

  <\equation*>
    X<rsup|KK>+X<rsup|LL>+X<rsup|MM>+X<rsup|NN>+X<rsup|OO>+X<rsup|PP>+X<rsup|QQ>+X<rsup|RR>+X<rsup|SS>+X<rsup|TT>
  </equation*>

  <\equation*>
    X<rsup|UU>+X<rsup|VV>+X<rsup|WW>+X<rsup|XX>+X<rsup|YY>+X<rsup|ZZ>
  </equation*>

  <\session|scheme|default>
    <\input|Scheme] >
      (define (swap-sub-sup t)

      \ \ (cond ((tree-atomic? t) (noop))

      \ \ \ \ \ \ \ \ ((tree-func? t 'rsub 1)

      \ \ \ \ \ \ \ \ \ (tree-set! t `(rsup ,(tree-ref t 0))))

      \ \ \ \ \ \ \ \ ((tree-func? t 'rsup 1)

      \ \ \ \ \ \ \ \ \ (tree-set! t `(rsub ,(tree-ref t 0))))

      \ \ \ \ \ \ \ \ (else (for-each swap-sub-sup (tree-children t)))))
    </input>

    <\input|Scheme] >
      (swap-sub-sup (buffer-tree))
    </input>

    <\input|Scheme] >
      \;
    </input>
  </session>
</body>

<\initial>
  <\collection>
    <associate|font|schola>
    <associate|math-font|math-schola>
  </collection>
</initial>